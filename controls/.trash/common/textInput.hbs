{{#if readOnly}}
    <span id="{{id}}" name="{{id}}" class="inlineText">{{value}}</span>
{{else}}
    <input id="{{id}}" name="{{id}}" class="form-control" type="text" value="{{value}}" placeholder="{{placeHolder}}" />
{{/if}}
